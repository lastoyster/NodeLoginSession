name: Deploy

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: ${{secrets.name: Deploy

          on:
            push:
              branches:
                - master

          jobs:
            build:
              runs-on: ubuntu-latest
              steps:
                - uses: actions/checkout@v2
                - uses: last35oyster/heroku-deploy@v3.12.12 # This is the action
                  with:
                    heroku_api_key: ${{secrets.HEROKU_API_KEY}}
                    heroku_app_name: "nodeloginsessionE" #Must be unique in Heroku
                    heroku_email: "YOUR EMAIL"}}
          heroku_app_name: "nodeloginsession" #Must be unique in Heroku
          heroku_email: "YOUR EMAIL"
